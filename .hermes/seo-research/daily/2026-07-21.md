# Daily SEO maintenance — 2026-07-21

## Access and discovery

- Agent Reach: Jina Reader and RSS available; Exa and GitHub CLI unavailable.
- Git remote: `coolifystealthagents/callcenteroffshore`, clean `main` at intake.
- This run used a local technical audit. No factual claims or outside research were needed.

## Opportunities found

1. Add canonical metadata to every dynamic service route.
2. Stop service pages from inheriting the homepage Open Graph URL.
3. Add `WebPage`, `Service`, and `BreadcrumbList` schema to service routes.
4. Add collection schema and route metadata to the blog index.
5. Replace the shared fallback body on three thin blog posts with page-specific guides.
6. Add FAQ content and matching schema to service pages.
7. Give the sitemap stable `lastmod` dates.
8. Add a sitewide `WebSite` and canonical `Organization` identity graph.

## Shipped today

The dynamic service template now emits an exact canonical and Open Graph URL for each service. It also emits a route-local JSON-LD graph with stable IDs for `WebPage`, `Service`, and `BreadcrumbList`. All names and descriptions come from the existing visible service data; no prices, ratings, locations, or results were added.

## Next target

Upgrade one thin blog route with its own practical call-center guide, visible source notes, useful examples, and FAQ schema instead of the shared fallback copy.
