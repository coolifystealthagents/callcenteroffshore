import { Header, Footer } from '../components';
import { researchPosts } from '../fleet-data';

export const metadata = { title: 'Research', description: 'Source-backed research for buyers of Philippines-based virtual assistant and outsourced support services.' };
export default function Research() { return <><Header /><main><section className="page-hero"><div className="container page-hero-grid"><div><p className="cco-kicker"><span /> Direct sources, clear limits</p><h1>Research for outsourcing decisions that need proof.</h1><p className="lead">Market numbers can provide context. They cannot replace a real role test, security review, pilot, or quality scorecard.</p></div><div className="page-hero-art"><img src="/visuals/quality-scorecard.svg" alt="Quality review scorecard for an outsourced support team" /></div></div></section><section className="blog-index"><div className="container">{researchPosts.map((post) => <a className="blog-feature" href={`/research/${post.slug}`} key={post.slug}><img src="/visuals/philippines-time-zone-handoff.svg" alt="Philippines outsourcing operations illustration" /><div><p className="cco-kicker"><span /> {post.wordCount.toLocaleString()} words / {post.readingMinutes} minute read</p><h2>{post.title}</h2><p>{post.excerpt}</p><b>Read the source-backed report ↗</b></div></a>)}</div></section></main><Footer /></> }
