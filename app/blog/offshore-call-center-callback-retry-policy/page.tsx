import { renderAug20Article, getAug20Metadata } from '../../aug20-content';
export const metadata = getAug20Metadata('offshore-call-center-callback-retry-policy');
export default function Page(){return renderAug20Article('offshore-call-center-callback-retry-policy');}
