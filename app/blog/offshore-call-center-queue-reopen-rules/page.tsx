import { renderAug20Article, getAug20Metadata } from '../../aug20-content';
export const metadata = getAug20Metadata('offshore-call-center-queue-reopen-rules');
export default function Page(){return renderAug20Article('offshore-call-center-queue-reopen-rules');}
