import { renderAug20Article, getAug20Metadata } from '../../aug20-content';
export const metadata = getAug20Metadata('call-center-customer-sentiment-boundaries');
export default function Page(){return renderAug20Article('call-center-customer-sentiment-boundaries');}
