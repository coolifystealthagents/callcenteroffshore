import { renderAug20Article, getAug20Metadata } from '../../aug20-content';
export const metadata = getAug20Metadata('philippines-call-center-identity-protection');
export default function Page(){return renderAug20Article('philippines-call-center-identity-protection');}
