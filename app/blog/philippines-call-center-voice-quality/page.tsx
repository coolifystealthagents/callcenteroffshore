import { renderAug20Article, getAug20Metadata } from '../../aug20-content';
export const metadata = getAug20Metadata('philippines-call-center-voice-quality');
export default function Page(){return renderAug20Article('philippines-call-center-voice-quality');}
