import { renderAug20Article, getAug20Metadata } from '../../aug20-content';
export const metadata = getAug20Metadata('call-center-authentication-failure-flow');
export default function Page(){return renderAug20Article('call-center-authentication-failure-flow');}
